services:
  prometheus:
    image: prom/prometheus:latest
    container_name: prometheus
    command:
      - "--config.file=/etc/prometheus/prometheus.yml"
      - "--storage.tsdb.path=/prometheus"
      - "--storage.tsdb.retention.time=15d"
      - "--web.enable-remote-write-receiver"
      - "--enable-feature=native-histograms"
    ports:
      - "9090:9090"
    volumes:
      - ${PWD}/docker/monitoring/prometheus/prometheus.yml:/etc/prometheus/prometheus.yml:ro
      - prom_data:/prometheus
    networks: [perf, app-network]

  loki:
    image: grafana/loki:latest
    container_name: loki
    command: -config.file=/etc/loki/local-config.yaml
    ports:
      - "3100:3100"
    volumes:
      - ${PWD}/docker/monitoring/loki/loki-config.yml:/etc/loki/local-config.yaml:ro
      - loki_data:/loki
    networks: [perf, app-network]

  promtail:
    image: grafana/promtail:latest
    container_name: promtail
    command: -config.file=/etc/promtail/config.yml
    volumes:
      - ${PWD}/docker/monitoring/promtail/promtail-config.yml:/etc/promtail/config.yml:ro
      - /var/run/docker.sock:/var/run/docker.sock:ro
    depends_on:
      - loki
    networks: [perf, app-network]

  grafana:
    image: grafana/grafana:latest
    container_name: grafana
    environment:
      GF_SECURITY_ADMIN_USER: admin
      GF_SECURITY_ADMIN_PASSWORD: admin
      GF_USERS_ALLOW_SIGN_UP: "false"
      TZ: Asia/Seoul
    ports:
      - "3001:3000"
    volumes:
      - grafana_data:/var/lib/grafana
      - ${PWD}/docker/monitoring/grafana/provisioning:/etc/grafana/provisioning
      - ${PWD}/docker/monitoring/grafana/dashboards:/etc/grafana/dashboards
    depends_on:
      - prometheus
      - loki
    networks: [perf, app-network]

networks:
  perf:
    name: perf-network
    driver: bridge
  app-network:
    external: true
    name: app-network

volumes:
  prom_data:
  grafana_data:
  loki_data:
