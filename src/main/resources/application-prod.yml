spring:
  datasource:
    url: ${DB_URL:jdbc:mysql://${DB_HOST}:${DB_PORT:3306}/${DB_NAME}?useUnicode=true&characterEncoding=UTF-8&serverTimezone=Asia/Seoul&allowPublicKeyRetrieval=false&useSSL=true&requireSSL=true}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: false
springdoc:
  api-docs:
    enabled: false
  swagger-ui:
    enabled: false

app:
  storage:
    type: oci
    oci:
      bucket: ${OCI_OBJECT_STORAGE_BUCKET}
      namespace: ${OCI_OBJECT_STORAGE_NAMESPACE}
      prefix: ${OCI_OBJECT_STORAGE_PREFIX:prod/}

custom:
  swagger:
    enabled: false

logging:
  level:
    root: WARN
    back: INFO
  file:
    name: logs/application.log
    max-size: 10MB
    max-history: 30
